<section id="search-results">
  <div class="sidebar">
    <div class="search-bar">
      <form action="/venues" id="search" method="GET" class="form">
        <input
          type="text"
          id="search-input"
          name="searchTerm"
          placeholder="Type a sport or click search for all results."
          value=""
        />
        <button class="search-button" id="search-button" type="submit">
          <i class="fa fa-search"></i>
        </button>
      </form>
    </div>
    <div class="filter">
      <input type="range" step="1" />
    </div>
  </div>
  {{#if err}}
    <div class="err">
      {{error}}, please try again.
    </div>
  {{else}}
    <div class="resultCount">Results Found: {{count}}</div>
    <div class="container">
      {{! Loop through the venue data fetched from backend }}
      {{#each venues as |ve|}}
        <div class="venue">
          <img class="image" alt="{{ve.venueName}}" src="{{ve.img}}" />
          <div class="padded">
            <h1 class="venue-name">{{ve.venueName}}</h1>
            <div class="details">
              <h3 class="rating">{{ve.venueRating}} / 5</h3>
              <h3 class="cost">US$ {{ve.price}} / hr</h3>
            </div>
            <p class="address">{{ve.venueAddress}}</p>
            <div class="timings">
              Timings:
              <ul>
                {{#each ve.venueTimings as |t|}}
                  <li>{{t.timeSlot}}</li>
                {{/each}}
              </ul>
            </div>
            <div class="timings">
              Sports:
              <ul>
                {{#each ve.sports as |s|}}
                  <li class="bg">{{s}}</li>
                {{/each}}
              </ul>
            </div>
            <button class="book-now">
              <p class="bk-text">Book Now</p>
              <i class="fa fa-arrow-right"></i>
            </button>
          </div>
        </div>
      {{/each}}
    </div>
  {{/if}}
</section>